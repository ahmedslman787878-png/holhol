rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // Demo mode: fully open to ensure the frontend local admin password works
    }
  }
}
